import styles from "@/styles/app/components/profile.module.scss";
import Image from "next/image";

export function EarlyPage() {
    return (
        <section className={styles.container}>
        </section>
    );
}
